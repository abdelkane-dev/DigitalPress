<?xml version="1.0" encoding="utf-8"?>
<!--
  android:usesCleartextTraffic="true" dans AndroidManifest.xml autorisait le
  HTTP non chiffré pour TOUTE l'app, y compris en build de production envoyée
  sur le Play Store — un vrai risque de sécurité (interception réseau) et un
  point souvent signalé lors du contrôle de sécurité du Play Store.

  Depuis l'activation du HTTPS (nom de domaine digitalpress-ml.com + Let's
  Encrypt), seuls les hôtes de développement local (émulateur = 10.0.2.2,
  localhost) restent autorisés en HTTP clair. L'IP du VPS (192.162.71.56) et
  les domaines de production ont été RETIRÉS : l'app n'y parle plus qu'en
  HTTPS. En cas de retour temporaire au HTTP pour un test, ne PAS réintroduire
  l'IP du VPS en clair — corriger le certificat à la place.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
