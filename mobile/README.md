# DigitalPress Mobile (Flutter) Boilerplate

This is a minimal Flutter app skeleton showing structure, basic auth and simple screens.

Run (after installing Flutter):

```bash
cd mobile
flutter pub get
flutter run
```
