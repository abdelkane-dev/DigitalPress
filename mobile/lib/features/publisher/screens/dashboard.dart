import 'package:flutter/material.dart';

class PublisherDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Publisher Dashboard')),
      body: Center(
          child: Text('Create and manage your publications (placeholder).')),
    );
  }
}
